# Resolve `source`d files relative to each script's own directory (SCRIPTDIR is
# a shellcheck magic token for the dir of the file being checked), so a relative
# `# shellcheck source=lib/foo.sh` needs no path prefix — which is how test/run
# reaches its lib/. (bin/lint passes -x to enable following at all.)
source-path=SCRIPTDIR
