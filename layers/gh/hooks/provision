#!/usr/bin/env bash
set -euo pipefail

# shellcheck source=/dev/null
. "$PARA_HELPERS"

# One gh login per project, whether or not the git layer is here to open
# git:before. Nothing else in this hook needs a clone.
# shellcheck source=shared-config
. "$PARA_LAYER_DIR/hooks/shared-config"
