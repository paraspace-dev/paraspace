return {
  -- Niche filetypes / syntaxes this workspace actually uses. A wider set from a
  -- fuller config (kitty, ledger, monit, openvpn, LaTeX/vimtex) is dropped here
  -- to keep the image lean; add back whatever your project needs.
  { "IndianBoy42/tree-sitter-just", ft = "just" },
}
