#!/usr/bin/env bash
set -euo pipefail
# Fills a point this layer's own provision hook opens, like the bundled git
# layer opens git:before around the operation it owns.

printf 'e2e-mod-point-before\n' >> "$HOME/owners-seen"
